spring:
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848
        prefix: order-service-provider
        file-extension: yml
  application:
    name: order-service-provider
